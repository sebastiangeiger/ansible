- name: Get launchctl services
  shell: "launchctl list | grep homebrew"
  register: services
  ignore_errors: True

- name: Ensure postgresql is started at login
  command: brew services start postgresql
  when: "'homebrew.mxcl.postgresql' not in services.stdout"
