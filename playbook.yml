- hosts: localhost
  vars:
    homebrew_taps:
      - Homebrew/bundle
    dotfiles_files:
      - .ackrc
      - .bin
      - .colordiffrc
      - .config
      - .git
      - .gitignore
      - .gitignore_global
      - .nvimrc
      - .tmux.conf
      - Brewfile



  tasks:
    - include: tasks/homebrew.yml
    - include: tasks/dotfiles.yml
